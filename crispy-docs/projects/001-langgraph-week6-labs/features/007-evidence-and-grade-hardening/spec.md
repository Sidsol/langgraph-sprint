---
feature: 007-evidence-and-grade-hardening
project: 001-langgraph-week6-labs
document: spec
status: complete
updated: 2026-05-16
---

# Spec: evidence-and-grade-hardening

## Goal
Ship the final grader-facing submission bundle: replace placeholder docs, commit canonical evidence logs, and add rubric-trace verification so the repo is submission-ready without any missing artifacts.

## Scope
- replace `README.md` with setup, demo, rubric crosswalk, evidence pointers, and repo tour
- replace `docs/architecture.md` with the short architecture write-up, Mermaid diagram, node roles, routing rules, retry summary, and HITL summary
- generate and commit four deterministic `docs/evidence/` scenarios plus per-folder README instructions
- add final graph-wiring assertions and a new rubric-trace pytest file over the committed evidence
- finalize the feature-level `spec.md` and `implementation-manifest.yaml` for the last feature in the roadmap

## Acceptance criteria
- `README.md` uses the exact assignment artifact wording and tells a grader where each artifact lives
- `docs/architecture.md` mirrors the canonical Mermaid and summarizes node roles, routing, self-correction, and HITL behavior
- `docs/evidence/` contains committed success, retry, reject, and escalation runs generated by the real agent
- `uv run pytest` passes with the new wiring and rubric-trace tests included
- `git check-ignore -v docs/evidence/...` shows the committed evidence is not ignored
