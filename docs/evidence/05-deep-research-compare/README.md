# 05-deep-research-compare

This committed run demonstrates the deep-research search path: `planner` selects `research_depth = deep`, `search_tool` fans out into multiple sub-queries, `citation_verifier` evaluates structured key facts, and approval publishes the sectioned markdown/email output. Thread ID: `t-evidence-005-deep`. Question: `Compare LangGraph and LangChain agent abstractions`.

## Regenerate
```powershell
uv run python scripts\regenerate_evidence.py 05-deep-research-compare
```

## Rubric artifact satisfied
- Structured research/publish evidence for the post-M6 deep research enhancement
- Additional proof that publish remains gated behind HITL approval in deep mode
