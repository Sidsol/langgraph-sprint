# 03-hitl-reject

This committed run reaches the approval interrupt on the calculator path and then rejects the draft, proving that no publish side effects occur after a rejection. Thread ID: `t-evidence-003-reject`. Question: `What is 7*8?`.

## Regenerate
```powershell
uv run python scripts\regenerate_evidence.py 03-hitl-reject
```

## Rubric artifact satisfied
- Human-in-the-loop checkpoint evidence (screenshot or log)
- Evidence of one human-in-the-loop interrupt in action
